//C++에서는 다양한 오버로딩 기법이 존재합니다.
//오버로딩 : 같은 이름의 다른 기능
// 
//1. 함수 오버로딩 :함수의 이름이 같지만, 매개변수의 순서, 개수,
//타입이 다를 경우 다른 함수로 취급합니다.

//2. 생성자 오버로딩 : 클래스 내의 생성자의 매개변수의 순서, 개수, 타입이
//다를 경우 다른 생성자로 취급합니다.
//매개변수가 없는 생성자(기본 생성자)가 없을 경우 클래스를 선언할 수 없습니다.
//생성자가 수정되었을 경우 기본 생성자가 없을 경우, 클래스 생성 시 반드시
//생성자가 요구하는 매개변수에 맞는 데이터를 넣어줘야 합니다.(MonsterStatus.h 예제 확인)

//Operation Overloading(연산자 오버로딩)
//클래스 내부에 있는 연산자를 대상으로 연산자 오버로딩을 작업할 경우, 객체 간의 연산을
//작업할 수 있게 도와줍니다.

int main()
{


	return 0;
}